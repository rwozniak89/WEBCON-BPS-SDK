const distPath = './dist';
const packagePath = './package';
const manifestPath = './src/manifest.json';
const srcPath = './src/**/*.js';

module.exports = {
    distPath,
    packagePath,
    manifestPath,
    srcPath,
};
