const del = require('del');
const { distPath, packagePath } = require('./const');

function clean() {
    return function clean() {
        return del([`${distPath}/**/*`, `${packagePath}/**/*`]);
    };
}

exports.clean = clean;
