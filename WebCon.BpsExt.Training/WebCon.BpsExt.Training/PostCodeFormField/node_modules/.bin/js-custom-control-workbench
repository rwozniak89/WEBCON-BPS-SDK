#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@webcon/js-custom-control-workbench/bin/www" "$@"
  ret=$?
else 
  node  "$basedir/../@webcon/js-custom-control-workbench/bin/www" "$@"
  ret=$?
fi
exit $ret
